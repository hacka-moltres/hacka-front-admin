import React, { memo, Fragment } from 'react';
import Insights from 'components/shared/Insights';

export default memo(() => (
  <Fragment>
    <h1>Olá, Fulano</h1>

    <Insights />
  </Fragment>
));
